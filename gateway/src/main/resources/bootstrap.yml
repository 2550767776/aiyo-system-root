spring:
  application:
    name: gateway-service
  cloud:
    inetutils:
      use-only-site-local-interfaces: false
      preferred-networks:
        - ^192\.168\.1\.\d*
      ignored-interfaces:
        - VirtualBox
        - SSTAP
